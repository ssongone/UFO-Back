server:
  port: 12346
spring:
  profiles:
    include:
      - mysql
      - security
      - redis
      - cloud
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true

  data:
    redis:
      repositories:
        enabled: false

  cache:
    type: redis



