spring:
  profiles:
    include:
      - mysql
      - security
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      repositories:
        enabled: false

  cache:
    type: redis

  redis:
    host: 43.202.241.133
    port: 6379
    expire: 60


